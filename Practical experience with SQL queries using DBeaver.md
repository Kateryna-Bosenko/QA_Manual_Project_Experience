[Completed tasks](https://docs.google.com/spreadsheets/d/17mCdDdHstFOcfl3R6sClfDDeT0xDryPdKRtWryCWms4/edit?usp=sharing)
---
## Executed advanced SQL queries on employee and company databases to extract, analyze, and aggregate data.

#### The following tasks:
   ##### Task 1. Select employees from the employees table and sort them by hire_date in descending order. Select `employee_no, first name, last name, and hire_date`.
   ##### Task 2. Select employees from the employees table who were born after 1960 (inclusive). Select `first name, last name, and birth_date`.
   ##### Task 3. Select employees from the employees table whose name begins with "Ma". Select `first name, last name, and birth_date`.
   ##### Task 4. Select `employee_no` and `city` from the `employeeTerritories` table.
   ##### Task 5. Count the number of employees from the `titles` table who hold the title `Engineer`. Name the resulting column `total_engineers`.
   ##### Task 6. Retrieve the number of employees for each city from the `employeeTerritories` table. Include only cities with more than 10 employees. Retrieve the number of employees and the name of the city.
   ##### Task 7. Select the first and last names of employees, their positions in the company. Retrieve the first name (first_name), last name (last_name) and position (title) using the `employees` and `titles` tables.
   ##### Task 8. Select the first and last names of employees whose salary will be between 50,000 and 60,000. Select the first name (first_name), last name (last_name) and salary (salary) using the `employees` and `salaries` tables.
